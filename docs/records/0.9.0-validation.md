<!-- SPDX-License-Identifier: MIT -->
<!-- Copyright (c) 2026 Gneiss contributors -->

# 0.9.0 场景创作工作流验收记录

## 结论

2026-08-28 已在 `feat/ver009-scene-authoring` 完成本地 Windows 验收。Editor 已形成“创建或打开场景、
编辑层级与首批组件、操作 Transform、撤销重做、保存并重新打开”的场景创作闭环。用户完成的人工
检查覆盖 Project Manager、场景显示、世界网格、Transform Gizmo 和 Inspector 交互；其余确定性行为
由自动测试和本地、远端构建矩阵覆盖。0.9.0 的既定完成条件已经满足。

## 自动化闭环

- Scene Instance 测试覆盖节点创建、重命名、重挂接、子树复制、删除、恢复及失败原子性。
- Editor 测试覆盖工程、保存、属性、命令历史、Gizmo 数学、XYZ 欧拉角转换和资产工作流。
- Editor、Project Manager、Temple、Lantern Gallery、Granit 平台及启动诊断 smoke 均通过。
- Lantern Gallery 工作流覆盖导入、场景节点创建、Undo/Redo、保存和重新加载。
- 静态安装测试覆盖 C11、C++20 和属性检查 Consumer。

## 本地 Windows 矩阵

| 配置 | 结果 | 测试 |
| --- | --- | ---: |
| Visual Studio 2022 Shared Debug + Granit + Editor + Tools | 通过 | 65/65 |
| Clang Shared Debug + Granit + Editor + Tools | 通过 | 65/65 |
| Clang Static Debug + Tools + 安装 Consumer | 通过 | 46/46 |
| Clang Shared Debug + Granit + Editor + Tools 关闭 | 构建通过 | 未启用测试 |

所有配置保持警告视为错误。Clang 首轮发现聚合初始化遗漏字段以及场景文件对话框错误依赖 Asset
Browser 条件编译；补齐显式初始化并解耦场景对话框后，上述矩阵重新通过。

## 远端手动矩阵

- [Linux Actions](https://github.com/synchronized/gneiss/actions/runs/33170871447)：通过。覆盖 Clang/GCC
  共享与静态核心矩阵，以及 Granit 共享/静态、Weston/Xvfb 无头 Editor 和图形示例闭环。
- [Windows Actions](https://github.com/synchronized/gneiss/actions/runs/33170875253)：通过。覆盖 MSVC
  共享/静态 Runtime，以及安装后的 C11/C++20 Consumer 共享/静态矩阵。

Linux 首轮运行发现 `debug_draw_list.cpp` 缺少 `<cstdint>` 直接包含；提交 `12be4a3` 修复后完整重跑
通过。Windows 仅报告 `ilammy/msvc-dev-cmd@v1` 的 Node.js 20 弃用提示，GitHub 强制使用 Node.js 24
后所有 Job 仍通过，不阻塞 0.9.0。
