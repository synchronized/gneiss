<!-- SPDX-License-Identifier: MIT -->
<!-- Copyright (c) 2026 Gneiss contributors -->

# AGENTS.md

## 适用范围

本文件适用于仓库根目录及其所有子目录。所有自动化代理、协作者和代码生成工具修改本项目时
都应遵守这些约定。子目录如有更具体的 `AGENTS.md`，其规则仅覆盖对应目录，并与本文件共同生效。

## 项目概览

Gneiss 是基于 Granit 构建的现代 C++20 游戏引擎。项目采用 Scene Tree（场景树）与 ECS
双栈架构，并通过 Service + RID 隔离渲染、网络、音频、物理等后端资源。

项目当前处于早期设计与搭建阶段，公共 API、ABI、构建结构和依赖方案尚未稳定。修改时优先保证
分层边界、所有权、错误语义和长期架构清晰，不得把规划能力描述为已经实现的功能。

## 通用沟通要求

- 对外说明、进度更新和总结使用中文。
- 代码注释和文档使用中文。
- 回答保持简洁、直接、可执行。
- 不展示内部思考过程；需要解释时提供简要判断依据或排查摘要。
- 不确定的设计问题先说明假设、影响和待确认项，不将猜测写成项目事实。

## 核心架构约束

### 分层与依赖方向

- Scene Tree 位于逻辑层，负责对象层级和开发者友好的运行时组织。
- ECS 位于数据层，负责实体、组件、系统的数据组织与调度。
- 场景节点只关联实体 ID，不直接持有服务或后端资源对象。
- ECS 组件保存纯数据或 RID，不持有 Granit、libuv 等第三方库的原生类型。
- 渲染、网络、音频和物理能力按独立 Service 划分，由服务管理资源生命周期和后端调用。
- 后端层只能被对应服务依赖，不得反向依赖 ECS、Scene Tree、编辑器或业务逻辑。
- 新增跨层依赖或改变长期模块边界前，应创建或更新 ADR。

### 公共接口分层

- `.h` 文件提供稳定的 C11 ABI，必须能由 C11 编译器独立包含。
- `.hpp` 文件提供 C++20 包装，基于 C API 提供强类型、移动语义和 RAII。
- C++ 包装层保持轻量，不建立与 C API 平行的第二套运行时状态。
- C API 使用 `gneiss_` 前缀，宏使用 `GNEISS_` 前缀。
- C++ API 位于 `gneiss` 命名空间。
- 第三方库的类型、头文件、句柄和编译定义不得通过普通公共接口泄漏。

### ABI、RID 与所有权

- C ABI 只使用定宽整数、显式布局的 C 结构体、函数指针和整数句柄。
- ABI 中不得出现 STL 类型、C++ 对象、模板、异常、虚函数或平台宽度不固定的裸 `long`。
- 错误通过结果码返回，不允许异常穿过 C ABI。
- 字符串和数组使用“指针 + 长度”，并明确编码、所有权和有效期。
- 可扩展描述结构包含 `struct_size`；已发布字段只在末尾追加，不重排。
- RID 使用 64 位整数表示，零值统一表示无效句柄，不得直接暴露指针或后端原生句柄。
- RID 表至少校验槽位 generation、资源类型和所属服务；销毁后旧 RID 必须失效。
- 新增资源接口时明确创建、销毁、所有权、线程安全和父资源失效行为。

## 代码与文档规范

- 修改代码前阅读 `docs/guides/coding-style.md`。
- C++ 实现和包装使用 C++20；公共 C 接口保持 C11 可表达性。
- 排版遵循根目录 `.clang-format`；静态检查遵循 `.clang-tidy`。
- 源文件和头文件保留统一的 SPDX 许可证与版权声明，不记录个人修改历史。
- 公共 API 使用简洁的中文 Doxygen 注释，重点说明所有权、生命周期、错误和线程安全。
- 第三方代码与自有代码隔离，不对第三方目录进行无关格式化或静态检查修复。
- 修改文档前阅读 `DOCUMENTATION_GUIDE.md`，保持 README、Guide、Reference、Concept、Roadmap、
  Plan、ADR 和 Record 的职责分离。
- 同一事实只维护一个权威来源；其他文档使用摘要和相对链接。
- 当前行为、未来计划和历史记录不得混写。

## 构建与测试原则

- 使用现代 target-based CMake，不在全局范围传播不必要的编译选项、宏和包含目录。
- 第三方依赖必须说明用途、版本、许可证、替代方案以及对构建和 ABI 的影响。
- 新增 API 或修复缺陷时覆盖成功、失败、生命周期和线程边界。
- 公共 `.h` 由 C11 编译器独立验证，公共 `.hpp` 由 C++20 编译器独立验证。
- RID 测试至少覆盖零值、类型错误、generation 失效、跨服务混用和重复销毁。
- 涉及公共 ABI、导出、安装或依赖传播时，应验证共享库、静态库、C consumer 和 C++ consumer。
- 不通过关闭警告、跳过测试或降低检查等级掩盖问题。
- 当前构建与测试命令以 `docs/guides/building.md` 为权威来源。开发 preset 默认启用警告并将
  警告视为错误；未落地的 CI 矩阵不得写成当前能力。

## 修改原则

- 修改前阅读相关源码、测试、构建文件和文档，并检查工作区已有修改。
- 保持改动聚焦，避免无关重构和大范围格式化。
- 不覆盖或回退用户已有修改，除非用户明确要求。
- 修改公共 API 时同步评估 C API、C++ 包装、测试、示例和对应文档。
- 优先扩展现有抽象，不创建含义重叠的第二套接口或运行时状态。
- 新功能应从最小可验证闭环开始，不提前实现没有真实使用场景的扩展能力。

## 推荐工作流

1. 一个完整路线图任务或可独立评审的功能使用一个特性分支；设计、实现、测试和文档等子阶段
   在同一分支持续完成，不为每个子步骤创建独立 Pull Request。
2. 较大任务先建立 Plan，明确目标、非目标、已确认决策、实施顺序、测试验收和未决问题；涉及长期
   架构取舍时同时建立 ADR。
3. 实施最小必要改动，同步补充测试和文档；每次完成一组连贯且已验证的改动后，直接创建聚焦的
   本地提交，不需要再次请求授权。
4. 开发期间优先运行格式检查、静态检查、相关构建和测试；高风险改动再扩大本地验证矩阵。
5. 默认在完整任务达到可评审状态后统一推送并创建一个 Pull Request。远端 Actions 仅支持手动触发，
   普通推送、Pull Request 和合并均不自动运行。
6. 本地无法覆盖平台差异、远端集成环境或 CI 配置时，可以在获得用户明确授权后，对同一特性分支
   手动触发阶段验证；修复失败时继续复用同一 Pull Request。
7. 发布或高风险合并前应对待合并提交手动运行所需 Actions 并确认通过；是否在合并后的 `main` 上
   再次手动验证，由发布工作流和用户授权决定。
8. 每次提交前运行 `git diff --check` 并检查提交范围；最终总结说明变更内容、验证结果、未验证项
   和剩余风险。
9. 本地提交已获得持续授权；推送、创建或合并 Pull Request、手动触发或重跑 Actions 仍须按本
   工作流执行，并获得用户明确授权。已有外部操作授权只在其明确覆盖的任务范围内生效。
