<!-- SPDX-License-Identifier: MIT -->
<!-- Copyright (c) 2026 Gneiss contributors -->

# Gneiss 项目文档规范

## 目的

本规范定义 Gneiss 文档的职责、目录、写作方式和生命周期。目标是让读者能够快速找到唯一可信的信息来源，并避免 README、架构文档、路线图和实施计划重复维护同一事实。

本规范参考 Granit 的文档组织方式，并根据游戏引擎项目的模块边界和当前开发阶段进行了精简。

## 核心原则

### 单一职责

每份文档只回答一个主要问题。入门、操作步骤、接口行为、设计原理、未来计划和历史记录应按职责拆分。

### 单一事实来源

同一项事实只在一份权威文档中完整维护。其他文档只提供简短摘要和相对链接，不复制会独立变化的内容。

### 区分当前、未来与历史

- Tutorial、Guide、Reference 和 Concept 只描述仓库当前可验证的行为。
- Roadmap 和 Plan 描述未来目标或正在实施的方案。
- ADR 记录已经作出的架构决策及其原因。
- Record 和 Changelog 记录已经发生的过程与结果。
- 未实现的能力必须明确标记为“计划”“草案”或“概念示意”。

### 面向读者组织

文档按读者要完成的任务组织，而不是简单复刻源码目录。术语、示例和链接应帮助读者从项目入口逐步到达具体主题。

## 文档类型

| 类型 | 回答的问题 | 主要内容 | 不应包含 |
| --- | --- | --- | --- |
| 项目 README | 这是什么，如何开始 | 定位、状态、特点、最小入口 | 完整文档索引、长期日志 |
| 文档中心 | 文档在哪里，从何读起 | 分类索引、推荐路径 | 实现细节 |
| Tutorial | 如何从零完成一个目标 | 连续步骤、预期结果 | 完整 API 枚举 |
| Guide | 如何解决一个具体问题 | 前置条件、步骤、验证、排错 | 架构演进历史 |
| Reference | 接口的准确行为是什么 | 参数、返回值、所有权、限制 | 未确认方案 |
| Concept | 系统为何这样设计 | 模型、边界、关系、权衡 | 逐次实施日志 |
| Roadmap | 先做什么，进展如何 | 阶段、优先级、状态摘要 | 详细实现步骤 |
| Plan | 某项能力如何实现 | 目标、非目标、步骤、验收 | 无限追加的完成日志 |
| ADR | 为什么选择某个方案 | 背景、决策、影响、替代方案 | 使用教程 |
| Record | 实际执行结果是什么 | 日期、环境、结果、差异 | 当前 API 定义 |

## 目录结构

```text
README.md                    # 项目入口
DOCUMENTATION_GUIDE.md       # 本规范
CHANGELOG.md                 # 版本变更记录
docs/
├─ README.md                 # 文档中心
├─ tutorials/                # 完整学习教程
├─ guides/                   # 面向任务的操作指南
├─ reference/                # API、格式和行为参考
├─ concepts/                 # 架构与原理
├─ roadmap.md                # 项目阶段与优先级摘要
├─ plans/                    # 未完成或实施中的计划
├─ decisions/                # 架构决策记录
└─ records/                  # 实施、测试、性能与迁移记录
```

只在有真实内容时创建对应目录和索引，不为空目录添加占位文件。现有内容应渐进迁移，避免仅为匹配结构进行大规模重命名。

## 文档入口与链接方向

```text
README.md
  └─ docs/README.md
       ├─ Tutorial / Guide
       ├─ Reference / Concept
       ├─ Roadmap → Plan
       └─ ADR / Record
```

- 根 README 只链接文档中心、快速开始所需页面和少量关键入口。
- `docs/README.md` 维护完整分类索引，根 README 不重复该索引。
- Roadmap 链接详细 Plan，Plan 不复制全局路线图。
- Concept 可以链接 ADR 解释决策来源，ADR 不承担当前架构说明。
- 仓库内文档统一使用相对链接；移动文件时必须检查入链和出链。
- 链接文字必须说明目标内容，避免使用“点击这里”等模糊措辞。

## 内容与排版

- 先写用途、结论或当前状态，再补充细节。
- 标题使用清晰的名词或任务描述，不连续使用“其他”“补充”“说明”。
- 并列事实使用无序列表，有先后关系的步骤使用编号列表。
- 表格用于字段、能力或方案对比；关系图只在明显提升理解时使用。
- 代码块必须标明语言；伪代码和 Mermaid 图中的规划能力必须标为概念示意。
- 代码示例、构建命令和配置必须能够对应当前仓库实现。
- 统一使用 ECS、实体 ID、RID、Scene Tree、Service 等项目术语；首次出现时给出中文含义。
- 中文与英文、数字、行内代码之间保持可读间距，专有名词遵循官方写法。
- 文档文件使用 UTF-8、LF 换行，不保留行尾空格。

## 篇幅建议

- 根 README 建议不超过 180 行。
- Tutorial、Guide、Reference 和 Plan 建议不超过 250 行。
- ADR 建议不超过 100 行。
- 超过 300 行时，必须检查是否混合了多个文档职责。

篇幅是拆分提示，不是硬性目标。拆分后的每份文档必须能独立说明自己的主题。

## 修改范围

| 发生的变化 | 通常修改的权威文档 |
| --- | --- |
| 项目定位或最小入口变化 | 根 README |
| 构建、测试或操作流程变化 | 对应 Guide |
| 公共 API、配置或行为变化 | 对应 Reference；必要时同步示例 |
| 场景树、ECS、服务或后端边界变化 | ADR；决策稳定后更新 Concept |
| 实施方案变化 | 对应 Plan |
| 阶段或优先级变化 | Roadmap；必要时同步 Plan 状态 |
| 测试、性能或迁移结果 | Record 或专用结果目录 |

修改代码时先确定受影响事实的权威来源，不机械更新所有上层文档，也不遗漏确实发生变化的使用说明。

## 文档生命周期

### 新建

1. 明确目标读者、主要问题和文档类型。
2. 搜索已有权威文档，避免创建重复入口。
3. 选择稳定文件名和目录。
4. 将新文档加入 `docs/README.md` 对应分类。
5. 明确当前能力与未来计划的边界。

### 实施中

- Plan 只维护当前方案、步骤、未决问题和验收状态。
- 可复用的重要决策写入 ADR。
- 命令输出、性能数据和逐次执行日志写入 Record 或结果目录。

### 完成或失效

1. 将已验证的长期事实写入 Reference、Guide 或 Concept。
2. Plan 保留简短完成状态和主要差异，不继续堆积日志。
3. ADR 通常不删除；被新决策替代时标记为“已取代”并链接新 ADR。
4. 失效 Plan 可归档，并注明替代文档。

## 文档模板

### Guide

```markdown
# 任务标题

## 适用场景
## 前置条件
## 操作步骤
## 验证结果
## 常见问题
```

### Plan

```markdown
# 任务编号：标题

## 状态
## 背景与目标
## 非目标
## 已确认决策
## 实施顺序
## 测试与验收
## 风险与未决问题
```

### ADR

```markdown
# ADR-000：决策标题

- 状态：提议 / 已接受 / 已取代
- 日期：YYYY-MM-DD

## 背景
## 决策
## 影响
## 替代方案
```

## 提交前检查

- 文档是否只有一个主要职责？
- 当前行为、未来计划和历史记录是否明确分离？
- 是否重复了其他权威文档中会持续变化的内容？
- 新文档是否已加入文档中心的正确分类？
- 描述、示例和命令是否与当前实现一致？
- 项目术语、标题层级和代码块语言是否一致？
- 相对链接、标题锚点和本地文件是否有效？
- 修改范围是否覆盖真正受影响的权威文档？

## Gneiss 当前映射

- 根 `README.md` 是项目定位、状态和最小入口的权威来源。
- `docs/README.md` 是全部项目文档的统一索引。
- `docs/concepts/architecture.md` 是场景树、ECS、服务和后端分层的权威来源。
- `docs/guides/coding-style.md` 是 C11 与 C++20 语言、命名、格式和接口边界的权威来源。
- 构建流程、公共 API、路线图和计划应在对应内容可验证后新增，不能将预期能力写成现状。
